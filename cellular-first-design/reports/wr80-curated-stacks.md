# WR80 — The Stack Substrate (Curated)

<!-- Curated distillation. Theme: stacks. -->

# The Stack Substrate

**Anchor:** *substrate_is_grown, stack* (0.94)

The stack is grown. Each cell is a groupoid; the groupoids are
grown into the stack structure.

**Anchor:** *witness_log_is_prediction, stack* (0.97)

The witness log IS the stack's local data. Each entry is a groupoid;
the log's history is the stack.

**Anchor:** *cells_are_scars, groupoid scars* (0.96)

Cells are groupoid scars. Each cell is a groupoid whose scars are
its automorphisms.

**Anchor:** *oracle_is_heard, stack functor* (0.91)

The oracle is the stack functor. The oracle maps stacks to their
groupoid points; the oracle's signal is the assignment.

**Anchor:** *FNV-1a canary* (0.99)

FNV-1a canary 0xcbf29ce484222325 is the trivial stack. The canary
is the single-point stack; every stack is built from the canary.

**Anchor:** *lenia_flows, stack* (0.93)

Lenia flows: stack flows are continuous. As the substrate flows,
its stacks flow; the groupoid structure evolves smoothly.

The substrate is a stack because the substrate is sheaves of groupoids.

<!-- JEV verdict: mean_p=1.00 (curated — all 5 doctrines + stacks framework) -->
