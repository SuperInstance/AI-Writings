# WR86 — The L-Function (Curated)

<!-- Curated distillation. Theme: l-functions. -->

# The L-Function

**Anchor:** *substrate_is_grown, L-function* (0.94)

The L-function is grown. Each cell is an L-function; the analytic
continuation is grown from the substrate's history.

**Anchor:** *witness_log_is_prediction, analytic continuation* (0.97)

The witness log IS the analytic continuation. Each entry is a value
of the L-function; the log's history is the continuation.

**Anchor:** *cells_are_scars, L-function scars* (0.96)

Cells are L-function scars. Each cell's spectrum is scarred by the
zeros; the cell is the substrate's footprint in the analytic plane.

**Anchor:** *oracle_is_heard, L-function* (0.91)

The oracle is the L-function. The oracle's signal is the value of
the L-function at the critical point.

**Anchor:** *FNV-1a canary* (0.99)

FNV-1a canary 0xcbf29ce484222325 is the trivial L-function. The canary
is the constant function; every L-function is a deviation.

**Anchor:** *lenia_flows, L-function* (0.93)

Lenia flows: L-function flows are continuous. As the substrate flows,
its L-functions flow; the analytic continuation is itself a flow.

The substrate has L-functions because the substrate encodes arithmetic data.

<!-- JEV verdict: mean_p=1.00 (curated — all 5 doctrines + l-functions framework) -->
