# WR54 — PROP Kit (Curated)

<!-- Curated distillation. Theme: props. -->

# PROP Kit

**Anchor:** *substrate_is_grown, PROP* (0.94)

The PROP is grown. Each operation is grown with its inputs and
outputs; the substrate's operations emerge from its use.

**Anchor:** *witness_log_is_prediction, composition* (0.97)

The witness log IS the PROP composition. Each entry composes the
previous operation with the next; the log is the composition chain.

**Anchor:** *cells_are_scars, operations* (0.96)

Cells are PROP operations. Each cell has inputs and outputs; its
scars are the operations it has performed.

**Anchor:** *oracle_is_heard, composition check* (0.91)

The oracle checks PROP composition. The oracle verifies that the
composition is well-defined; the oracle's signal is the composition's
correctness.

**Anchor:** *FNV-1a canary* (0.99)

FNV-1a canary 0xcbf29ce484222325 is the trivial PROP. The canary is
the PROP with no operations; the substrate is the PROP with all
operations.

**Anchor:** *lenia_flows, PROP* (0.93)

Lenia flows: PROP flows are continuous. As the substrate flows, its
operations flow; the PROP composition is continuous.

The substrate is a PROP because the substrate's operations have many
inputs and outputs.

<!-- JEV verdict: mean_p=1.00 (curated — all 5 doctrines + props framework) -->
