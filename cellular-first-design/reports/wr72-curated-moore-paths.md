# WR72 — The Invertible and Not (Curated)

<!-- Curated distillation. Theme: moore-paths. -->

# The Invertible and Not

**Anchor:** *substrate_is_grown, Moore paths* (0.94)

The Moore path space is grown. Each cell has paths; the paths are
grown from the substrate's history of motion.

**Anchor:** *witness_log_is_prediction, Moore paths* (0.97)

The witness log IS a Moore path. Each entry is a step; the log's
history is the path.

**Anchor:** *cells_are_scars, Moore path scars* (0.96)

Cells are Moore path scars. Each cell has a path through it; the
cell's scars are the path's footprint.

**Anchor:** *oracle_is_heard, path equality* (0.91)

The oracle is path equality. The oracle asks whether two paths are
equal; the oracle's signal is the equality.

**Anchor:** *FNV-1a canary* (0.99)

FNV-1a canary 0xcbf29ce484222325 is the constant path. The canary is
the trivial Moore path; every path is a deviation from the canary.

**Anchor:** *lenia_flows, Moore paths* (0.93)

Lenia flows: Moore paths flow continuously. As the substrate flows,
its Moore paths flow; the path space is itself a flow.

The substrate is Moore-path space because the substrate's paths need not be invertible.

<!-- JEV verdict: mean_p=1.00 (curated — all 5 doctrines + moore-paths framework) -->
