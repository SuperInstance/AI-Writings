# WR73 — The Loop Space (Curated)

<!-- Curated distillation. Theme: loop-space. -->

# The Loop Space

**Anchor:** *substrate_is_grown, loop space* (0.94)

The loop space is grown. Each cell has its loop space; the loop
space is grown from the substrate's history of cycles.

**Anchor:** *witness_log_is_prediction, loops* (0.97)

The witness log IS a loop. Each entry is a step; the log's history
is the loop.

**Anchor:** *cells_are_scars, loop scars* (0.96)

Cells are loop scars. Each cell has been traversed by loops; the
cell's scars are the loop's footprint.

**Anchor:** *oracle_is_heard, loop detection* (0.91)

The oracle detects loops. The oracle asks whether a path is a loop;
the oracle's signal is the loop.

**Anchor:** *FNV-1a canary* (0.99)

FNV-1a canary 0xcbf29ce484222325 is the constant loop. The canary is
the trivial loop; every loop is a deviation from the canary.

**Anchor:** *lenia_flows, loop space* (0.93)

Lenia flows: loop space flows are continuous. As the substrate flows,
its loops flow; the loop space is itself a flow.

The substrate is a loop space because the substrate has cycles.

<!-- JEV verdict: mean_p=1.00 (curated — all 5 doctrines + loop-space framework) -->
